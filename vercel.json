{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://hpp-canada-inventory.onrender.com/api/:path*" },
    { "source": "/:path*", "destination": "/index.html" }
  ]
}
